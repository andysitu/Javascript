function PrimeTest(a){
  //Have fun..
  if (a < 2)
    return false;
  for (var i = 2; i < a/2; i++) {
    if (!(a % i))
      return false;
  }
  return true;
};




function PrimeTest(a){
  for (var i = 2; i < a / 2; i++) if (!(a % i)) return false;
  return a >= 2;
};