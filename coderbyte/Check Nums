function CheckNums(num1,num2) { 

  if (num1 === num2) {
   return "-1"; 
  }
  return num1 < num2; 
         
}

function CheckNums(num1,num2) { 

  return num1 == num2 ?  "-1" :  num1 < num2
         
}