function BinaryConverter(str) { 

  str = str.split("").reverse();
  var sum = 0;
  str.forEach(function(ele, i, arr){
   sum += Math.pow(2, i) * ele 
  })
  return sum; 
         
}






function BinaryConverter(str) { 
 return parseInt(str, 2);     
}