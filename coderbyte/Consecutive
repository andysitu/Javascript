function Consecutive(arr) { 
  arr = arr.sort(function(a,b){ return a-b; })
  var sum = 0;
  for (var i = arr.length -1; i > 0; i--) {
  	sum += arr[i] - arr[i-1] - 1;  
  }
  return sum; 
         
}