var isSquare = function(n){
  var value = Math.floor(Math.sqrt(n));
  if (value * value == n)
    return true;
  else {
    return false; // fix me
  }
}



function isSquare(n) {
	return Math.sqrt(n) % i === 0;
}